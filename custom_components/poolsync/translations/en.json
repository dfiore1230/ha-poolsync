{
  "title": "PoolSync",
  "config": {
    "step": {
      "user": {
        "title": "Connect to PoolSync",
        "description": "Enter the device address. If you have a known User ID (UUID), include it to improve push-link success.",
        "data": {
          "base_url": "Base URL (e.g., http://172.16.42.218)",
          "user_id": "User ID (optional)",
          "poll_seconds": "Polling interval (seconds)",
          "request_timeout": "Request timeout (seconds)"
        }
      }
    },
    "error": {
      "pushlink_failed": "Could not obtain password via push-link. Try again."
    }
  }
}